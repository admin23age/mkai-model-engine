# Context — `gsd-add-tests`

Shared GSD context lives at `context/gsd/`. Skill-specific context (planning
artefacts, captured discussions) gets written here at runtime by the
`/gsd-add-tests` workflow.
